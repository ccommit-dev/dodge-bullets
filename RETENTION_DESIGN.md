# 리텐션 기획 — 이탈 지점 지도와 콘텐츠 집중 8종

> 목적: 유저가 "왜 지금 나가는가 / 왜 내일 돌아오는가 / 돌아와서 무엇에 집중하는가"를
> 구간별로 답한다. 기존 기획(IDLE_REDESIGN · LIVEOPS_DESIGN · CRUMBLE_GAP_DESIGN)과
> 출시 전 점검표 14항목이 만든 장치를 **전제**로, 아직 비어 있는 자리만 채운다.

## 0. 이미 있는 리텐션 장치 (전제)

| 구간 | 있는 것 |
|---|---|
| D0 첫 세션 | 온보딩 순차 개방(§8), 코치 3단계, 다음 목표 스트립, 첫 원정 슬로모 튜토리얼, 첫 클리어 2배 |
| D1 복귀 | 방치 정산 모달(+병목 안내·대장간 CTA), 방치 캡·파견 귀환 로컬 푸시, 출석 7일 |
| 세션 내 | 요일 균열·토벌령·랭크 시험, 별점, 시너지 편성, 파견, 펫 부화, AUTO·×2 |
| 벽 | DPS 벽 배너 3택(재련·성급·환생), 환생 복귀 24h ×2 부스트, 따라잡기(P1) |
| 장기 | 원정 일지 10칸, 도감 영구 능력치, 성급·재련 무한 축, 커스터마이즈 상점 |

## 1. 이탈 지점 지도 — 가설과 공백

| 구간 | 이탈 가설 | 현재 대응 | 공백 |
|---|---|---|---|
| **D0 · 첫 3분** | 첫 보스 전에 "이 게임이 뭘 주는지" 못 느낌 | 코치·탭 유도 | 첫 세션 안에 **원정 개방(Stage 5)** 까지 못 가면 서사가 시작조차 안 됨 — 곡선 미검증 |
| **D0 · 세션 종료** | 닫을 이유는 있어도 **돌아올 이유가 명시되지 않음** | 첫 보스 후 방치 안내 1회 | "닫으면 얼마 쌓이는지"를 숫자로 예고하는 장치 없음 |
| **D1 · 복귀 직후** | 정산 받고 나면 **무엇을 해야 할지** 다시 찾아야 함 | 병목 안내(1개 변수) | 정산 → 균열 → 토벌령 → 강화 → 파견의 **하루 루틴이 한 화면에 없음** |
| **D3~D7 · 벽** | 벽이 "얼마나 남았는지" 안 보여 좌절 | 벽 배너 3택 | 정량 미터가 없어 "조금만 더"가 성립하지 않음 |
| **세션 내 · 집중** | 탭 4개 + 상점 5분류 + 이벤트 4탭 + 마이페이지 — **어디를 볼지 분산** | 각 화면의 개별 안내 | 안내가 여러 곳에 흩어져 있고 서로 우선순위가 없음 |
| **D7~D14 · 단기 목표** | 일지(장기)와 토벌령(하루) 사이 **주 단위 목표**가 없음 | 랭크 시험(주간 1개) | 요일 균열이 만든 주간 리듬을 목표로 묶는 장치 없음 |
| **실패 순간** | 비트·원정 실패 후 즉시 재도전 비용(스태미나·이동)이 이탈 트리거 | 재도전 버튼 | 실패 직후 "무료 1회"와 "다음엔 이렇게"가 없음 |
| **외부 동기** | 서버 없어 자랑할 곳이 없음 | 칭호·스킨(본인만 봄) | 결과를 밖으로 들고 나갈 카드 없음 |

## 2. 기획 8종 (우선순위 순)

### A. 데일리 루틴 보드 — 허브 상단 5칸 ⭐
- 사냥터 헤더 아래 고정 스트립: `정산 → 균열(0/3) → 토벌령 → 강화 1회 → 파견`
- 각 칸은 **실제 상태에서 파생**(정산: idleClaimedAt 오늘, 균열: EventSave.riftAttempts, 토벌령: claimed, 강화: firstClearDates.forge, 파견: expeditions.length). 새 저장 데이터 없음
- 5칸 완료 시 소량 보석(10) + "오늘 루틴 완료" 도장 — **하루의 끝**을 만들어 준다
- 탭하면 해당 화면으로 이동 = 안내가 곧 내비게이션
- 규모 소 · 근거: 방치형에서 D1 복귀 직후의 첫 30초를 결정하는 표준 장치

### B. 오늘의 추천 1개 — 우선순위 엔진 ⭐
- 흩어진 안내(병목·벽 배너·목표 스트립·귀환 배너)를 **하나의 함수** `recommendNext(progress, titans, events)`로 통합
- 우선순위: 수령 가능(파견 귀환·일지·조각 승급 가능) > 오늘 남은 무료(균열·토벌령) > 성장 병목(R/M/T) > 장기(성벽·재련)
- 허브 최상단 배너 1개만 노출, 실행 버튼 1개. 다른 안내는 배너가 없는 상태에서만 보조로
- 규모 소 · 근거: 점검표 #1이 지적한 정보 과부하를 **출시 후에도** 유지하는 장치

### C. 복귀 워밍업 버프 — 첫 5분 골드 ×2
- 2시간 이상 이탈 후 접속하면 5분간 사냥 골드 ×2 (idleBoostUntil과 별개, `warmupUntil`)
- 정산 모달의 "받기" 버튼이 곧 버프 시작 = 정산 직후 사냥터에 머무를 이유
- 하루 최대 3회(연속 접속 남용 방지). 데이터 1개
- 규모 소 · 근거: 세션 시작 5분의 활동량이 세션 길이를 결정한다

### D. 벽 미터 + 단일 추천 행동
- 보스 실패 시 "보스 HP 대비 현재 DPS로 시간 내 깎는 비율" `wallRatio`를 계산해 배너에 **62% → 100%** 게이지로 표시
- 3택 대신 **가장 싸게 갭을 메우는 행동 1개**를 계산해 제시(동료 레벨업 N회 vs 성급 1단계 vs 재련 1회 — 골드·조각 보유 기준으로 도달 가능한 것 우선)
- 규모 소 · 근거: "조금만 더"가 성립하려면 숫자가 있어야 한다

### E. 첫 세션 5분 곡선 + 세션 종료 예고 카드
- 시뮬레이터(balance-sim)에 **신규 유저 첫 10분 시나리오** 추가: 탭+미아만으로 Stage 5 도달 시간을 측정 → 5분 초과 시 Stage 1~4 보스 HP·제한시간 완화
- 앱 백그라운드 전환(visibilitychange hidden) 직전 마지막 프레임에 "지금 닫으면 8시간 후 **+N골드**" 카드 1회(computeIdleYield로 예측) — 첫 3세션에만
- 규모 소 · 근거: D0 종료 시점에 돌아올 이유를 숫자로 심는다

### F. 주간 도전 3종 회전
- 요일 균열과 같은 주차 키(weekKey)로 3개 회전: 예) "황금 균열 3회", "별 2개 갱신", "보호구 강화 2단계"
- 보상은 조각팩·가속권(기존 재화). 일지(장기)와 토벌령(하루) 사이의 **7일 리듬**
- EventSave에 `weekly: string[]` 하나
- 규모 소

### G. 실패 순간 완화 — 무료 재도전 1회 + 한 줄 팁
- 비트 실패: 같은 곡 즉시 재도전은 스태미나 미차감 1회(곡당 하루 1회)
- 원정 실패: 사망 원인(유도탄·분열 조각·낙사)을 월드가 기록해 결과 화면에 "다음엔 이렇게" 한 줄
- 규모 소 · 근거: 실패 직후 30초가 이탈률이 가장 높은 순간

### H. 결과 공유 카드
- 클리어·별점 갱신·칭호 획득 시 canvas로 1080×1350 카드 생성(캐릭터 + 별 + 전투력 + 칭호), Web Share API / 앱인토스 공유로 내보내기. 서버 불필요
- 규모 중 · 근거: 서버 없는 게임의 유일한 바이럴 축이자 커스터마이즈(스킨·칭호) 구매 동기

## 3. 측정 — 기획이 맞았는지 알 수 있게

- `game/telemetry.ts`: errlog와 같은 로컬 링버퍼(200건)로 **세션 시작/종료·정산 클릭·루틴 완료·벽 도달·재도전** 이벤트 기록. 백업 코드처럼 설정 메뉴에서 복사 가능 — 서버 없이도 테스터 로그를 받는다
- 판정 기준(첫 업데이트 후 확인): D1 복귀 시 루틴 보드 3칸 이상 완료율 50%+, 벽 도달 후 24h 내 재도전률, 첫 세션 Stage 5 도달률

## 4. 우선순위 표

| 순서 | 기획 | 규모 | 왜 먼저 |
|---|---|---|---|
| 1 | A 데일리 루틴 보드 | 소 | D1 복귀 첫 30초를 결정 |
| 2 | B 오늘의 추천 1개 | 소 | 분산된 안내를 하나로 — 집중의 근본 |
| 3 | C 복귀 워밍업 버프 | 소 | 정산 직후 사냥터 체류 |
| 4 | E 첫 세션 곡선 + 종료 예고 | 소 | D0→D1 전환 |
| 5 | D 벽 미터 | 소 | D3~D7 이탈 |
| 6 | F 주간 도전 | 소 | 7일 리듬 |
| 7 | G 실패 완화 | 소 | 실패 직후 30초 |
| 8 | H 공유 카드 | 중 | 외부 동기 · 커스터마이즈 구매 연결 |

1~3은 한 세트(복귀 → 루틴 → 추천), 전부 새 저장 데이터가 거의 없고 기존 상태에서 파생된다.

## 5. 미결정 — 승인 필요

1. 워밍업 버프 배율 ×2와 5분 — 방치 ×2 부스트와 같은 값이라 "부스트가 흔해지는" 인상 우려. ×1.5/10분 대안
2. 루틴 완료 보상을 보석(하드 화폐)으로 줄지 조각으로 줄지 — 보석이면 하루 10 = 월 300, 상점 최저가(80)와 비교해 결제 압박 완화
3. 공유 카드의 앱인토스 공유 API 가용성 — 미확인 시 Web Share + 이미지 저장으로 대체
