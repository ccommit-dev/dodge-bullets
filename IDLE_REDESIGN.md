# 방치형 역기획 — 『쿠키런: 크럼블』 구조를 dodge-bullets에 이식

**작성일** 2026-08-20 · **대상** dodge-bullets (앱인토스 미니앱)
**레퍼런스** 쿠키런: 크럼블 (데브시스터즈, 2026-07-30 출시, 방치형 RPG)
**구현 상태** `feat/idle-linkage` 브랜치에 9단계 전부 구현 완료 — 10절 참조

---

## 0. 이 문서의 전제

- 레퍼런스 영상(`RX-2lLR3GzY`, "방치형RPG 쿠키런:크럼블 2시간 플레이 리뷰")은 **재생하지 않았다.**
  제목·출시 리뷰 기사·위키 기준으로 확인된 **시스템 구조만** 근거로 삼았다.
  영상에만 나오는 수치·UX 디테일은 이 문서에 없다.
- 확인된 크럼블의 구조: 오프라인 성장, 쿠키 69종 / 펫 54종, **최대 12종 동시 편성 덱**,
  메인 스테이지 · **아레나(PvP)** · **임플란트 타워** · **일일 던전**, **무한 장비 뽑기**.
- 산출물은 기획 문서다. 구현 코드는 포함하지 않는다.

---

## 1. 현재 구조 스냅샷

### 1.1 4개 콘텐츠

| 코드 | 이름 | 성격 | 핵심 파일 |
|---|---|---|---|
| `titans` | 사냥터 | 클리커/방치 RPG (스테이지·보스·동료·스킬) | `src/TitansGame.tsx`, `src/titans/model.ts` |
| `dodge` | 화살 원정 | 액션 회피, 4스테이지 고정 | `src/App.tsx`, `src/game/stages.ts` |
| `forge` | 대장간 | 확률 강화 (+0~+15) | `src/ForgeGame.tsx`, `src/forge/model.ts` |
| `beat` | 비트박스 연습실 | 리듬 수련 (킥/햇/스네어/파이어/스로트) | `src/BeatGame.tsx`, `src/beat/rpg.ts` |

### 1.2 공유 진행도 — `CharacterProgress`

`src/progression/model.ts`가 4개 콘텐츠의 결과를 한 저장소로 모은다.
전투력은 이미 4개 전부를 참조한다:

```
combatPower = level×20 + equippedWeaponLevel×28 + bestForgeLevel×14
            + titanBestStage×7 + totalSkillMastery×3
```

### 1.3 현행 방치 로직 (baseline)

`src/TitansGame.tsx:196-203` 한 곳이 전부다.

| 항목 | 현재 값 |
|---|---|
| 누적 대상 | **골드만** |
| 산출식 | `killGold(stage, false, false) × awaySeconds / 10` |
| 의미 | 활성 대비 고정 **초당 0.1킬** 상당 (DPS와 무관) |
| 시간 캡 | **4시간** |
| 배율 요소 | 없음 |
| 복귀 연출 | 토스트 1줄 (`방치 보상 +N GOLD`) |

`killGold(stage) = 5 × 1.26^(stage-1) × 지역배율(1 ~ 5)`

### 1.4 확인된 설계 구멍 3개

| 필드 | 상태 | 근거 |
|---|---|---|
| `unlockedHuntingArea` | **쓰기 전용 · 읽는 곳 없음** | `progression/storage.ts:56`, `TitansGame.tsx:329`에서 갱신만 |
| `inheritanceCrystals` | 환생으로 쌓이나 **소비처 없음** | `CharacterStatus.tsx:131`에서 증가, 이후 숫자 표시만 |
| `evolutionPath` | **전투 효과 없음** | `CharacterStatus.tsx:163` 아바타 외형 분기에만 사용 |

이 3개가 이번 역기획의 재활용 자원이다. 신규 상태를 만들지 않고 여기에 얹는다.

---

## 2. 크럼블 → dodge-bullets 매핑

| 크럼블 | 이 프로젝트 | 상태 |
|---|---|---|
| 메인 스테이지 (방치 전투) | `titans` 사냥터 | **이미 있음** — 방치 코어로 승격 |
| 무한 장비 뽑기 | `forge` 대장간 | **이미 있음** — 확률표·보호권 존재, 무한 반복 루프만 추가 |
| 일일 던전 | `EventCenter` 오늘의 토벌령 + 차원 균열 3회 | **이미 있음** — 방치 재화와 연결 필요 |
| 임플란트 타워 | `dodge` 화살 원정 | **부분** — 4스테이지 유한. 무한 등반 모드 추가 |
| 쿠키+펫 시너지 덱 | `beat` 스킬 × `titans` 스킬 슬롯 | **미연결** — 슬롯 구조(`skillInventory.equipped`)는 이미 정의됨 |
| **아레나 (PvP)** | **없음** | **구현 불가** — 2.1절 참조 |

### 2.1 아레나가 불가능한 이유 (명시적 제약)

저장은 `src/game/toss.ts`의 `storageGet`/`storageSet` — **로컬 키-값 저장소뿐이고 서버가 없다.**
실시간·비동기 PvP는 이 구조에서 만들 수 없다.

**대체안: 「명예의 전당 — 그림자 대전」**

- 상대는 실제 유저가 아니라 **결정론적 가상 상대**: `seed = hash(userHash + weekKey + rank)`로
  `combatPower` 기대치를 생성한다. 주차마다 상대 셋이 고정된다.
- 자기 자신의 과거 스냅샷(환생 직전 `combatPower`)을 "그림자"로 저장해 도전 대상에 섞는다.
- 보상은 방치 배율(3절 `M`)에 주간 한정 가산.
- **PvP라고 표기하지 않는다.** 유저에게 "주간 랭크 시험"으로 제시한다. 서버가 생기기 전까지
  실제 대인전을 흉내 내면 신뢰를 잃는다.

---

## 3. 구조 스파인 — 방치 공식 하나로 4개를 묶는다

4개 콘텐츠에 각각 오프라인 보상을 붙이지 않는다.
**방치 산출은 titans 하나가 낸다. 나머지 3개는 그 공식의 서로 다른 변수를 조절한다.**

```
초당 방치 산출 = killGold(S) × R × M   ,  누적 상한 = T 시간

  S  현재 사냥터 스테이지        ← titans가 올린다 · 상한은 dodge가 연다
                                  (최고 기록이 아니라 `TitansSave.stage`. 게이트는 이 값을 막는다)
  R  방치 효율 계수 (기본 0.10)  ← beat 수련이 올린다
  M  산출 배율 (기본 1.00)       ← forge 무기 + 환생 결정이 올린다
  T  누적 시간 캡 (기본 8h)      ← dodge 원정 진척 + 출석이 늘린다
```

각 콘텐츠가 **서로 다른 변수 하나**를 담당하므로, 어느 하나를 안 해도 게임이 멈추지 않고
전부 하면 곱연산으로 보상받는다. 이것이 크럼블식 "여러 콘텐츠 → 하나의 성장선" 구조다.

### 3.1 변수별 담당 콘텐츠

| 변수 | 담당 | 기존 필드 재사용 | 없을 때 |
|---|---|---|---|
| `S` 스테이지 | titans | `TitansSave.stage` (**현재 스테이지**) | 방치 안 됨 (코어) |
| 상한 지역 | **dodge** | **`pioneeredArea` (`unlockedHuntingArea` 대체)** | 지역 경계에서 정체 |
| `R` 효율 | **beat** | `beatSkills`, `skillPoints` | 0.10 고정 |
| `M` 배율 | **forge** + 환생 | `bestForgeLevel`, **`inheritanceCrystals` (싱크 신설)** | 1.00 고정 |
| `T` 캡 | **dodge** + 출석 | `dodgeBestStage` | 8h |

---

## 4. 콘텐츠별 연계 설계

### 4.1 `titans` — 방치 코어

**역할** 방치 산출의 유일한 생산자. 액티브 플레이는 "가속"이지 "필수"가 아니다.

**변경점**

1. **오프라인 바스켓 확장** — 현행 골드 단일 → 골드 + EXP + 강화석.
2. **지역 게이트 도입** — `HUNTING_AREAS`(초원 → 숲 → 폐허 → 협곡 → 심연) 경계에서
   `pioneeredArea`가 부족하면 진행 정지. 다음 지역은 **dodge 원정으로만** 열린다.
   현재는 `unlockedHuntingArea`를 아무도 읽지 않아 게이트가 존재하지 않는다.
3. **복귀 화면** — 토스트 1줄을 방치 정산 모달로 교체.
   경과 시간 / 획득 바스켓 / 캡 도달 여부 / "지금 부족한 변수" 1개를 짚어 준다
   (예: `T` 캡에 걸렸으면 → "화살 원정 Stage 3을 깨면 방치 시간 +1h").

**오프라인 산출 테이블 (제안)**

| 재화 | 산출식 | stage 10 · 8h · M=1 · R=0.10 기준 |
|---|---|---|
| 골드 | 초당 `killGold(S) × R × M` | 약 16.7만 (`killGold(10)=58` 기준) |
| EXP | 초당 `S × 0.015 × M` | 4,320 |
| 강화석 | 시간당 `min(2 + floor(S/4), 20) × M` | 32 |

`R` 기본 0.10은 현행 `/10`과 동일하다 — **기존 밸런스를 깨지 않는 것이 출발점**이다.

### 4.2 `forge` — 무한 장비 뽑기 · 배율 `M`

**크럼블 대응** 무한 장비 뽑기 (반복 파밍 부담 완화 장치)

**이미 있는 것** 16단계 확률표, `steady`/`rush` 모드, 보호권, 조각 제작(`shardSwordCost`)

**변경점**

1. **골드 싱크로 연결** — 현재 `defaultForgeSave().gold = 1_000_000`으로 **자체 골드를 들고 시작**한다.
   이걸 `sharedCoins`로 통합해 **방치 골드의 주 소비처**로 만든다. 방치 → 강화 → DPS 상승 →
   방치 효율 상승의 코어 루프가 여기서 닫힌다.
2. **무한 뽑기 모드** — +15 도달 후에도 반복 가능한 "재련" 루프. 결과는 등급이 아니라
   `M`에 붙는 소수점 배율(옵션 롤). 파밍 종료 지점을 없앤다.
3. **배율 공식**

   ```
   M = 1 + bestForgeLevel × 0.06
         + inheritanceCrystals × 0.01
         + floor(towerBestFloor / 100) × 0.05     (성벽 기여 상한 +0.50)
         + reforgeRank × 0.02
         + (검성이면 0.15)                          (전체 상한 3.0)
   ```

   `inheritanceCrystals`에 **처음으로 소비처가 생긴다.**

   **결정 계수가 0.01인 이유** — 현재 환생 1회 지급량은
   `max(3, floor(sqrt(titanBestStage) × 3))` ([CharacterStatus.tsx:126](src/CharacterStatus.tsx:126))이다.
   최초 환생 조건인 Stage 30에서 이미 **16개**가 들어온다. 개당 +0.05로 잡으면 첫 환생만으로 `M +0.80`,
   forge 최대치(+0.90)와 합쳐 상한 3.0을 곧바로 친다. 0.01이면 첫 환생 +0.16,
   누적 100개(대략 5회차)에서 +1.00으로 완만하게 붙는다.

**주의** 강화 실패로 등급이 떨어질 때 방치 산출이 같이 깎이면 체감이 지나치게 나쁘다.
그래서 `M`은 **`bestForgeLevel`(최고 기록) 기준**으로 계산하고,
현재 장착값 `equippedWeaponLevel`은 액티브 DPS에만 쓴다.

### 4.3 `beat` — 시너지 덱 레이어 · 효율 `R`

**크럼블 대응** 쿠키 + 펫 시너지 조합. 크럼블에 1:1 대응 콘텐츠가 없으므로 억지로 끼우지 않고
"덱 구성" 축을 맡긴다.

**이미 있는 것**

- `beatSkills`: kick / hat / snare / fire / throat 5종, 각 0~99
- `titans/model.ts`의 `skillInventory.equipped`: `starter / linkA / linkB / finisher / passive` **5슬롯**
  → 5:5로 정확히 맞는다. 지금은 서로 연결돼 있지 않다.

**변경점**

1. **슬롯 해금** — 비트 스킬 숙련이 titans 스킬 슬롯을 연다.
   `kick→starter`, `hat→linkA`, `snare→linkB`, `fire→finisher`, `throat→passive`.
   각 슬롯 해금 조건: 해당 스킬 숙련 5 / 15 / 30 → 슬롯 레벨 1/2/3.
2. **오프라인 자동 시전** — 장착 스킬이 방치 중에도 도는 것으로 간주해 `R`을 올린다.

   ```
   R = 0.10 + (장착 슬롯 레벨 합) × 0.012                (상한 0.25)
   ```

   전 슬롯 3레벨(합 15)이면 산식값 0.28 → 상한 0.25로 절삭. **방치 효율 최대 2.5배**이며,
   이는 순전히 리듬 게임을 얼마나 했는지로 결정된다.
3. **명성(`fame`)의 용도** — 4.5절 주간 랭크 시험의 시드 가중치로 쓴다.

**설계 의도** 비트박스는 4개 중 가장 "게임을 직접 해야 하는" 콘텐츠다.
그래서 **방치의 효율 자체**를 맡긴다 — 액티브 플레이가 방치를 강화하는 크럼블의 핵심 감각.

### 4.4 `dodge` — 개척 + 임플란트 타워 · 게이트와 `T`

**크럼블 대응** 임플란트 타워 (무한 등반)

**현재 문제** dodge는 exp/재료만 주고 **구조적 훅이 하나도 없다.**
`dodgeBestStage`·`dodgeBestScore`는 뱃지 조건과 이벤트 카운터로만 쓰인다.
4개 중 유일하게 다른 콘텐츠에 영향을 못 미친다. **이번 역기획의 실제 설계 작업은 여기다.**

**변경점**

1. **지역 개척 게이트 (핵심)** — 개척도를 dodge가 소유한다.
   `STAGES`는 4개, `HUNTING_AREAS`는 5개 — 정확히 맞물린다.

   > **필드 주의** 기존 `unlockedHuntingArea`는 이름과 달리 **지역 인덱스가 아니라 스테이지 번호**를 담고 있다
   > (`titans.bestStage`, `clearedStage + 1`을 `Math.max`로 누적). 그대로 재해석하면 Stage 30 유저의 값 30이
   > "지역 30 개방"으로 읽혀 게이트가 영구히 무력화된다. 그래서 **`pioneeredArea: 1..5` 필드를 신설**하고
   > 기존 필드는 폐기한다. 마이그레이션은 7절 참조.

   | 화살 원정 | 클리어 시 개방되는 사냥터 | 지역 배율 |
   |---|---|---|
   | (기본) | 새벽 초원 (1–5) | 1.00 |
   | Stage 1 외곽 초소 | 그림자 숲 (6–10) | 1.45 |
   | Stage 2 붉은 협곡 | 붉은 폐허 (11–15) | 2.05 |
   | Stage 3 왕실 사격장 | 용암 협곡 (16–23) | 3.10 |
   | Stage 4 검은 성문 | 심연의 성 (24+) | 5.00 |

   방치로 스테이지 5까지 밀면 벽에 막히고, **회피 게임을 30초 클리어해야 다음 지역이 열린다.**
   지역 배율이 1 → 5로 뛰므로 개척 1회의 가치가 크다.
   서사도 맞는다 — "정찰병이 길을 뚫으면 사냥터가 열린다."

2. **방치 시간 캡 `T`**

   ```
   T = 8h + min(4, dodgeBestStage) × 1h
         + min(2, floor(출석 연속일 / 3))h
         + (수호자면 2h)                                (상한 14h)
   ```

   `dodgeBestStage`는 성벽 등반으로도 올라가므로 일반 원정 4스테이지분으로 자른다.
   성벽의 기여는 `T`가 아니라 `M`이다 — 한 콘텐츠가 두 변수를 먹으면 공식이 무너진다.

   현행 4h는 하루 한 번 접속하는 미니앱 이용 패턴에 짧다.

3. **무한 등반 모드 「끝없는 성벽」** — 4스테이지 클리어 후 해금.
   `STAGES[3]`의 패턴을 층마다 `speedMul`/`spawnMul` 1.04배씩 올려 무한 생성.
   기록은 `dodgeBestScore`. 층수 100당 `M` +0.05 (상한 +0.5).
   → 크럼블의 임플란트 타워 = 실력이 방치 배율로 환산되는 창구.

### 4.5 일일 · 주간 루프

**이미 있는 것**

- `EventCenter.tsx` — 오늘의 토벌령 3종 / 차원 균열 3회 / 주간 원정 8포인트
- `AttendanceModal.tsx` — 7일 출석판

**변경점**

1. **일일 던전 = 차원 균열 재정의** — 현재 `combatPower × (18~24)` 난수를 표시할 뿐 보상이 없다.
   3회 도전을 **"방치 2시간 즉시 정산권"** 으로 바꾼다. 크럼블 일일 던전의 역할(재료 집중 수급)과 일치한다.
2. **일일 미션을 4개 축으로 재편** — 지금은 titans/dodge/forge 3개뿐이고 beat가 빠져 있다.
   `S 올리기 / 지역 개척 / 강화 시도 / 비트 수련` 4개로 맞춘다.
3. **주간 랭크 시험** (2.1절 그림자 대전) — 주간 원정 탭에 배치. 보상은 `M` 주간 가산.

---

## 5. 방치가 **주지 않는** 것 (경계선)

방치가 모든 걸 주면 4개 콘텐츠가 전부 죽는다. 연계의 실체는 여기에 있다.

| 재화 | 방치 | 이유 |
|---|---|---|
| 골드 | 지급 | 방치의 주 산출물. 소비처는 forge |
| EXP | 지급 | 레벨은 시간 보상이어도 무방 |
| 강화석 | 소량 지급 | 매일 강화 1~2회분만. 파밍은 액티브로 |
| **제련 조각 (`shards`)** | 미지급 | 주면 대장간이 사라진다 |
| **비트 SP / 명성** | 미지급 | 주면 리듬 게임이 사라진다 |
| **견갑 조각** | 미지급 | 출석·이벤트 전용 리텐션 재화 |
| **붉은 보석** | 미지급 | 결제·이벤트 전용 |
| **지역 개척** | 미지급 | dodge 전용. **방치로 절대 넘을 수 없는 유일한 벽** |

원칙: **방치는 "양"을 준다. "다음 단계"는 액티브만 준다.**

---

## 6. 환생 · 계승 레이어

**현재** Stage 30(+환생당 20) 도달 시 환생 → `inheritanceCrystals` 증가 → **아무 데도 안 쓰임**.
`evolutionPath` 3종(검성/수호자/공명술사)도 아바타 외형만 바꾼다.

**변경점**

1. `inheritanceCrystals` → `M` 배율 (4.2절, 개당 +0.01).
2. `evolutionPath`에 방치 특화 효과 부여 — 3개 변수 중 하나씩을 맡는다.

   | 진화 | 효과 |
   |---|---|
   | 검성 | `M` +0.15 · 액티브 치명타 |
   | 수호자 | `T` +2h · 견갑 효과 강화 |
   | 공명술사 | `R` +0.03 · 비트 슬롯 레벨 상한 +1 |

   현재 UI 설명("치명타·공격 특화", "생존·견갑 특화", "스킬·비트 특화")과 방향이 어긋나지 않는다.

---

## 7. 데이터 모델 영향

`CharacterProgress` **신규 필드는 3개뿐**이다. 나머지는 기존 필드에 의미를 부여한다.

| 필드 | 처리 |
|---|---|
| `unlockedHuntingArea` | **폐기** — 스테이지 번호를 담고 있어 지역 인덱스로 재사용 불가 (4.4절 참조) |
| 신규 `pioneeredArea: 1..5` | 개척도. titans 자동 갱신 없음, **dodge 클리어로만 증가** |
| `inheritanceCrystals` | 소비처 부여 (`M`, 개당 +0.01) |
| `evolutionPath` | 전투 효과 부여 |
| `bestForgeLevel` | `M` 계산의 주 입력 |
| `equippedWeaponLevel` | 액티브 DPS 전용으로 역할 한정 |
| `beatSkills` | titans 스킬 슬롯 해금에 사용 |
| `sharedCoins` | **지갑 단일 진실 소스**로 승격 — 방치 정산·대장간이 여기로 들어온다 |
| 신규 `idleClaimedAt: number` | 방치 정산 시각 (`titans.lastActiveAt`에서 승격) |
| 신규 `towerBestFloor: number` | 끝없는 성벽 최고 층 |
| 신규 `reforgeRank: number` | 무한 재련 등급 |
| 신규 `attendanceStreak: number` | 출석 연속일 (`T` 보너스) |

**`titanSkillSlots`는 만들지 않았다.** 슬롯 해금 레벨은 `beatSkills`에서,
장착 여부는 기존 `TitansSave.skillInventory.equipped`에서 파생된다.
상태를 새로 저장하면 두 곳이 어긋날 수 있어 계산으로 대체했다.

### 7.1 v4 → v5 마이그레이션 — 개척도 환산

`PROGRESSION_VERSION`을 **4 → 5**로 올린다. 핵심은 개척도 변환 한 줄이다.

```
pioneeredArea = HUNTING_AREAS.indexOf(huntingArea(unlockedHuntingArea)) + 1
```

`unlockedHuntingArea`에 들어 있는 값은 스테이지 번호이므로, 기존 `huntingArea(stage)` 함수로
**그 스테이지가 속한 지역까지**를 개척 완료로 인정한다. Stage 30 유저 → `pioneeredArea = 5`(전부 개방),
Stage 8 유저 → `2`(그림자 숲까지). **이미 도달했던 지역은 그대로 돌려주고**(소급 잠금 없음),
그 이후부터는 게이트가 정상 작동한다.

값을 그대로 옮기지 않는 이유는 4.4절의 필드 주의 항목과 같다 — 그대로 두면
`Math.max` 누적 특성상 기존 유저에게 게이트가 **영구히 걸리지 않는다.**
그러면 4개 콘텐츠 순환이 신규 계정에서만 성립한다.

`forge`의 자체 `gold`를 `sharedCoins`로 통합하는 것이 **가장 파급이 큰 변경**이다.
`defaultForgeSave().gold = 1_000_000`을 그대로 이관하면 초반 인플레가 나므로 별도 밸런싱이 필요하다.

---

## 8. 구현 순서 (권장)

| 단계 | 내용 | 위험도 |
|---|---|---|
| 1 | 방치 공식 `S·R·M·T` 를 `progression/idle.ts` 단일 모듈로 분리 (현행 값 그대로) | 낮음 |
| 2 | 방치 복귀 모달 + 바스켓 확장 (골드 → 골드/EXP/강화석) | 낮음 |
| 3 | **dodge 지역 개척 게이트** — `pioneeredArea` 신설 + v4→v5 환산 | **중간** (진행 차단 리스크) |
| 4 | `T` 캡 확장 (4h → 8h+) | 낮음 |
| 5 | beat → titans 스킬 슬롯 연결, `R` 가변화 | 중간 |
| 6 | 환생 결정 · 진화 효과 → `M` 연결 | 낮음 |
| 7 | forge 골드 통합 + 무한 재련 | **높음** (경제 재밸런싱) |
| 8 | 끝없는 성벽 (무한 등반) | 중간 |
| 9 | 일일 던전 재정의 · 주간 랭크 시험 | 중간 |

3단계를 먼저 넣는 이유: **dodge가 죽은 콘텐츠라는 것이 현재 가장 큰 구조적 문제**이고,
게이트 하나로 4개 콘텐츠가 처음으로 순환하기 때문이다.

**선행 결정** 3단계는 9.2(하드 게이트 vs 소프트 게이트)가 정해지기 전에는 착수할 수 없다.
두 방식은 `pioneeredArea`를 읽는 지점이 달라진다 — 하드는 titans 스테이지 진행 자체를 막고,
소프트는 `killGold`의 지역 배율만 잠근다. 1·2·4단계는 이 결정과 무관하므로 먼저 진행 가능하다.

---

## 9. 결정 사항

구현 시점에 내린 판단과 근거다.

1. **forge 골드 통합 → 통합했다.**
   대장간 지갑을 `sharedCoins`로 합쳐 방치 → 강화 → `M` → 방치 루프를 닫았다.
   "부담 없는 도박" 감각은 두 장치로 남겼다 — `M`은 현재 등급이 아닌 `bestForgeLevel`
   기준이라 실패해도 방치 산출이 깎이지 않고, 무한 재련은 실패 시 조각을 돌려준다.
   구 기본값 1,000,000을 그대로 옮기면 인플레가 나므로, **실제 강화 기록이 있는
   세이브만 잔고를 이관**하고 나머지는 개업 자금 50,000만 지급한다.

2. **개척 게이트 → 하드 게이트 + 즉시 탈출구.**
   소프트 게이트(지역 배율만 제한)는 벽으로 느껴지지 않아 dodge를 다시 죽은 콘텐츠로
   되돌린다. 대신 막히는 순간 화살 원정으로 바로 보내는 버튼을 띄운다 — 30초면 뚫린다.
   저장된 스테이지가 상한보다 앞서 있으면 로드 시 경계로 되돌린다.

3. **그림자 대전 고지 → UI에 명시.**
   "실제 유저와의 대전이 아닙니다"를 패널 본문에 넣고, 탭 이름도 아레나가 아닌
   **주간 랭크 시험**으로 붙였다. PvP를 흉내 내지 않는다.

4. **방치 골드의 목적지 → 공유 지갑.**
   초안은 사냥터 골드에 넣는 것을 전제했으나, 그러면 대장간이 소비처가 되지 못한다.
   방치는 `sharedCoins`(대장간), 액티브 전투는 `TitansSave.gold`(동료·스킬)로 갈랐다.
   방치가 액티브 플레이를 대체하지 않으면서 루프는 닫힌다.

### 남은 사안

- **광고 연동** — README상 광고 미포함. 방치 2배 보상은 방치형의 표준 수익 지점이므로
  `remove-ads` 상품(`economy/productCatalog.ts`, 현재 `visible: false`)과 함께 재검토 필요.
- **레퍼런스 정밀도** — 영상 미시청. 크럼블의 실제 방치 산출률·캡·정산 UX는 확인되지 않았다.
  본 문서의 수치는 **현행 코드 밸런스에서 출발한 자체 설계**이며 크럼블 수치의 복제가 아니다.
- **밸런스 실측** — 수치는 손계산과 단위 검증만 거쳤다. 장시간 플레이 곡선은 미검증.

---

## 10. 구현 결과

`feat/idle-linkage` 브랜치. 8절 9단계 전부 구현했다.

### 새 파일

| 파일 | 역할 |
|---|---|
| `src/progression/idle.ts` | `S·R·M·T` 공식 단일 모듈 · 병목 진단 |
| `src/IdleReturnModal.tsx` | 귀환 정산 모달 (카운트업 · 변수 게이지 · 병목 안내) |
| `src/AreaUnlockBanner.tsx` | 지역 개척 전체화면 연출 (성문 개방) |
| `src/events/shadowArena.ts` | 결정론적 그림자 상대 생성·판정 |
| `src/ui/sfx.ts` | 공용 효과음 (정산·개척·게이트·슬롯·성벽·재련·균열) |
| `src/idle.css` | 신규 UI 스타일 (App.css가 3,000줄을 넘어 분리) |

### 브라우저 실측으로 잡은 결함 3건

문서만으로는 드러나지 않았고, 실제로 돌려 보고서야 나왔다.

1. **개척 게이트가 전혀 걸리지 않았다.** `migrateLegacyProgress`가 매 로드마다
   `titans.bestStage`로 개척도를 재부여해, 초안이 경고한 "게이트 영구 무력화"가
   마이그레이션 경로로 되살아났다. 소급 부여를 v5 이전 레코드 1회로 제한했다.
2. **`sharedCoins`가 리로드마다 사라졌다.** 같은 함수가 레거시 코인 키 값으로
   `sharedCoins`를 덮어써, 방치 정산·대장간 이관 금액이 전액 증발했다(77만 → 0).
   레거시 키를 권위가 아닌 하한으로 바꿨다. 지갑을 `sharedCoins`로 통합하기 전에는
   두 값이 항상 같이 움직여 잠복해 있던 버그다.
3. **정산 모달 숫자가 0에 멈췄다.** 백그라운드 탭에서는 `requestAnimationFrame`이
   돌지 않아 카운트업이 0에 머문다 — 유저에게는 보상을 못 받은 것으로 보인다.
   최종값 스냅 폴백을 넣었다.

### 검증한 것

- `npx tsc --noEmit` · `npx eslint .` · `vite build` 전부 통과
- 방치 정산: Stage 5 · 9시간 캡 → 공유 골드 38,880 / EXP 2,430 / 강화석 27.
  `killGold(5)=12 × 0.1 × 32,400` 손계산과 일치. 수령 후 잔고 반영·리로드 생존 확인
- 개척 게이트: 개척도 1 · 사냥터 Stage 12 → 로드 시 Stage 5로 클램프
- 비트 연계: 킥 30·하이햇 15·스네어 5 → 슬롯 3/2/1, 활성 합 6 → `R` 17.2%
- 대장간 이관: 구 잔고 777,000 → 공유 골드, 재이관 차단, +15에서 재련 패널 노출
- 균열: 2시간 즉시 정산 정확히 지급, 3회 제한 동작
- 그림자 대전: 주차 고정 상대 3인 생성, 승리 보상·클리어 상태 저장
- 성벽: 원정 4스테이지 완주 시 해금, 250층 → `M` +0.10, 진입 시 층 HUD 표시

### 게이트 차단 모달 — 실제 보스 처치로 검증

로드 시 클램프 경로만 확인했던 부분을 실제 전투로 끝까지 돌렸다.
개척도 1(상한 Stage 5)에서 10웨이브를 비우고 보스를 처치한 결과:

- 게이트 모달 정상 노출 — "AREA LOCKED · 그림자 숲 · 개방 시 획득 배율 ×1.45"
- **저장된 스테이지가 5에서 움직이지 않았다** (판정을 증가보다 앞으로 옮긴 수정이 동작)
- `bestStage` · `titanBestStage`도 포화되지 않음
- 보스 골드는 정상 지급 (+353) — 막혀도 잡은 보상은 준다
- 탈출구 버튼이 화살 원정으로 이동, 개척 보드에 다음 목표 표시

### 레이아웃 — 320px 계측 감사

브라우저 페인이 프레임을 합성하지 않아 스크린샷은 찍지 못했다.
대신 모든 신규 컴포넌트의 기하를 계측했다(뷰포트 이탈 / 텍스트 잘림 / 탭 타깃 44px /
깨진 이미지). 최대치 세이브(Stage 60 · 재련 20 · 결정 80)로도 돌렸다.

잡은 것: `.idle-card`가 `overflow:hidden`으로 App.css의 세로 스크롤을 덮어써
작은 화면에서 하단 버튼에 손이 닿지 않던 문제, `.pioneer-row` 39px ·
`.shadow-list button` 32px 탭 타깃 위반. 전부 수정 후 0 이슈.

**여전히 육안 리뷰가 필요한 것** — 색 대비, 여백 리듬, 애니메이션 타이밍은
계측으로 잡히지 않는다. 이를 위해 **dev 전용 QA 패널**을 넣었다:

```
npm run dev 후 http://localhost:5173/?qa=1
```

정산 모달 3상태 / 게이트 차단 / 개척 연출(지역 5종)을 실제 컴포넌트로 바로
띄우고, 인게임에서만 닿는 상태(게이트 직전 · 성벽 해금 · 무한 재련 · 20시간
방치)를 원클릭으로 시딩한다. `import.meta.env.DEV` 격리로 프로덕션 번들에는
JS·CSS 모두 흔적이 남지 않는다(빌드 산출물 grep으로 확인).

### 전용 아트 에셋

자리표시자(`event-chest.png` 재사용, CSS 그라디언트 오브)를 전용 SVG 8종으로
교체했다 — `public/ui/idle/`: 귀환 정산 문장 / EXP 오브 / 개척 깃발 /
봉인 성문 / 성벽 / 모루 / 그림자 인장 / 차원 균열. 게임 팔레트 기준 벡터라
해상도 독립적이고 총 36KB. `prefers-reduced-motion` 지원.

---

## 11. 밸런스 시뮬레이션

`scripts/balance-sim.mjs` — `src/progression/idle.ts`를 **그대로 번들해서** 돌린다.
상수를 복제하지 않으므로 공식이 바뀌면 결과도 따라 바뀐다. 스테이지 진행은 고정
속도 가정이 아니라 실제 `monsterHp` / `heroDps` / `killGold`로 전투를 돌려 정한다.

```bash
node scripts/balance-sim.mjs --days 90
```

### 90일 결과 (하루 2회 접속 · 12시간 간격)

| | 방치형 (다른 콘텐츠 안 함) | 균형형 (4개 다 함) |
|---|---:|---:|
| 최종 Stage | **1** | 46 |
| 최종 레벨 | 66 | 436 |
| 일일 골드 (30일차 → 90일차) | 61.0K → **61.0K** | 3.41B → 28.39B |
| 누적 골드 | 5.33M | 893.08B |
| R 최대 도달 | 미도달 | 25일 |
| T 최대 도달 | 미도달 | 6일 |
| 전 지역 개척 | 미도달 | 10일 |

### 확인된 것

- **개척 게이트의 페이싱은 적절하다.** 90일간 4회 통과, 10일차에 전 지역 개방.
  게이트 때문에 정체한 날은 없었다 — 30초짜리 원정이라 막힌 당일 뚫린다.
- **진행을 실제로 막는 것은 게이트가 아니라 기존 사냥터 밸런스다.**
  `monsterHp`가 1.38^s, `killGold`가 1.26^s로 HP가 더 빠르게 늘어
  90일 중 87일이 DPS 벽이었다. 이 시스템이 만든 벽이 아니다.
- `R`·`T`는 설계대로 상한에 도달한다(25일 / 6일).

### 결정 (2026-08-21)

아래 1번 사안은 **선택지 (a) 현행 유지**로 확정했다 (사용자 지시: "밸런스는 1로 우선 설정").
방치는 양(골드·EXP·강화석)을 주고 스테이지 진행은 액티브 전용으로 남긴다.
`M` 상한 문제(2번)도 현행 수치를 유지하고 라이브 지표를 본 뒤 재론한다.

### 검토 기록 — 당시의 판단 근거

1. **방치형 플레이어의 `S`가 영구 고정된다.** 방치 정산은 골드·EXP·강화석만 주고
   사냥터 스테이지를 올리지 않는다. `killGold(S)`가 지수 항인데 `S`가 안 움직이므로,
   방치만 하는 플레이어의 수입은 **30일차 61.0K에서 90일차까지 그대로**다.
   유일한 성장선인 `M`은 대장간 상한(+15 → +0.90)에 묶여 ×1.54에서 멈춘다.

   5절의 "방치는 양을 준다, 다음 단계는 액티브만 준다" 원칙에는 부합하지만,
   방치형 장르의 전제("손을 안 대도 캐릭터가 성장한다")와는 어긋난다.
   레퍼런스인 크럼블은 오프라인에도 스테이지가 진행된다.

   선택지: (a) 현행 유지 — 액티브 게임에 방치 보상을 얹은 것으로 포지셔닝,
   (b) 방치 중에도 개척 상한까지 스테이지를 아주 느리게 진행,
   (c) `M` 상한을 실질 도달 가능하게 낮추고 방치 성장선을 `M`에 집중.

   **권장: (b).** 개척 상한이 이미 방치의 천장 역할을 하므로 게이트 설계를
   해치지 않으면서 장르 전제를 회복한다.

2. **`M` 상한 3.0은 사실상 도달 불가다.** 시뮬레이션에서 90일간 ×1.54에 머물렀다.
   결정(+0.01/개)·성벽(100층당 +0.05)·재련(2억 골드/회)이 모두 후반 콘텐츠라
   상한이 구속 조건으로 작동하지 않는다. 상한을 낮추거나 기여치를 올려야 한다.
