# 라이브 운영 기획 — 밸런스 · 집중도 · 결제

**작성일** 2026-08-27 · **전제** [IDLE_REDESIGN.md](IDLE_REDESIGN.md) §11 시뮬레이션 결과
**범위** 기획 문서. 구현 로드맵은 §5.

---

## 0. 요약

세 문제를 하나의 축으로 푼다: **동료 등급·성급 시스템**이
① 후반 DPS 곡선(밸런스), ② 수집·육성 장기 목표(집중도), ③ 확정 구매 상품(결제)을
동시에 담당한다. 별도 시스템 셋을 만들지 않고 하나가 세 역할을 하는 구조다.

결제는 **확률형(가챠) 전면 배제** — 전부 확정 구매·선택권으로 설계한다 (§3.1).

---

## 1. 밸런스 패치

### 1.1 시뮬레이션이 확인한 문제 (90일, `scripts/balance-sim.mjs`)

| # | 문제 | 데이터 |
|---|---|---|
| B1 | 방치형 플레이어의 수입 영구 정체 | 30일차 61.0K → 90일차 61.0K (S가 안 움직임) |
| B2 | DPS 벽이 사실상 유일한 진행 제약 | 90일 중 87일 벽 (몬스터 HP 1.38^s > 골드 1.26^s) |
| B3 | 환생이 명목상 존재 | 환생해도 사냥터가 리셋되지 않아 "다시 오르는 즐거움"이 없고, 결정 보상(+0.16)도 미미 |
| B4 | M 상한 ×3.0 미도달 | 90일간 ×1.54 — 단, 시뮬레이터가 무한 재련을 돌리지 않은 한계도 있음(§1.5) |

### 1.2 패치 P1 — 방치 따라잡기 진행 (B1 해결)

> **결정 변경 제안**: 이전에 "선택지 1(현행 유지)"로 확정했던 사안이다.
> 이번 기획으로 아래 절충안을 다시 제안한다 — 승인 필요.

방치 정산 시 사냥터 스테이지가 **최고 기록 −5까지만** 자동 진행된다.

```
방치 진행: 시간당 +1 스테이지, 상한 = min(개척 상한, titanBestStage − 5)
```

- **신기록은 액티브 전용으로 남는다** — "다음 단계는 액티브만"(IDLE_REDESIGN §5) 원칙 유지
- 방치형 플레이어도 killGold(S)의 지수 성장을 일부 누리므로 수입 정체 해소
- P3(환생 리셋)과 맞물려 "환생 → 방치가 이전 기록 근처까지 자동 복구 → 액티브로 신기록"
  이라는 방치형 표준 프레스티지 사이클이 완성된다

### 1.3 패치 P2 — DPS 벽의 재정의 (B2 해결)

벽 자체는 없애지 않는다 — 벽이 재련·환생·동료 성급(§2)의 **소비 동기**다.
대신 벽을 "막힘"이 아니라 "다음 행동 안내"로 바꾼다:

1. **벽 감지 배너**: 보스 제한시간 실패 2회 연속 시
   "지금 뚫는 법: 재련 +0.02 / 동료 성급 / 환생(결정 +N)" 3택 배너 노출
2. **벽 보상**: 각 지역에서 최초 벽 도달 시 1회 보석 지급 (벽 = 실패감 → 이정표로 반전)
3. 몬스터 HP 지수는 유지 (1.38). 곡선을 완화하면 벽 주기가 늘어져 소비 동기가 죽는다.
   후반 DPS는 §2 동료 성급이 채운다

### 1.4 패치 P3 — 환생 리워크 (B3 해결)

| 항목 | 현행 | 변경 |
|---|---|---|
| 조건 | titanBestStage ≥ 30 (+회차당 20) | **벽 도달 지역 ≥ 3** (Stage 표기보다 체감 명확) |
| 리셋 | 없음 (명목상) | **사냥터 리셋**: stage 1, 동료 레벨 1, 사냥터 골드 0 |
| 보존 | — | 개척도·최고기록·동료 성급·재련·공유 재화 전부 보존 |
| 보상 | 결정 `√best×3` (개당 +0.01) | 결정 동일, **개당 +0.02** (첫 환생 +0.32) |
| 복귀 속도 | — | P1 따라잡기 + "환생 후 24h 방치 효율 2배" 버프 |

### 1.5 시뮬레이터 확장 (검증 도구)

- 무한 재련·환생을 시뮬 루프에 포함시켜 B4를 재측정한다
  (현재 M 미도달 판정은 재련 미포함 상태의 수치라 과장돼 있을 수 있음)
- P1~P3 구현 후 동일 시드로 90일 재실행해 전후 비교표를 이 문서에 추가한다

---

## 2. 집중도 — 동료 등급·성급 + 도감

### 2.1 동료 등급 (기존 6인 소급 부여)

| 동료 | 등급 | 근거 (현행 baseCost/해금) |
|---|---|---|
| 스카우트 미아 | R (희귀) | 50G / Stage 1 |
| 궁수 레온 | R | 320G / Stage 3 |
| 마법사 세라 | SR (영웅) | 2.2K / Stage 6 |
| 기사 가렌 | SR | 18K / Stage 10 |
| 용기사 아리 | SSR (전설) | 160K / Stage 16 |
| 암살자 녹스 | SSR | 1.4M / Stage 24 |

등급은 성급 상한과 조각 단가만 결정한다 (R ★3 / SR ★4 / SSR ★5).
**기존 유저의 동료는 전원 ★1로 소급** — 잃는 것 없음.

### 2.2 성급(★) — 후반 DPS 곡선의 주인공

```
DPS 배율: ★1 ×1 → ★2 ×1.6 → ★3 ×2.6 → ★4 ×4.2 → ★5 ×7
승급 조각: ★2 20개 → ★3 50개 → ★4 120개 → ★5 300개  (동료 전용 조각)
```

조각 수급 (전부 비과금 경로 보유 — §3 원칙):

| 소스 | 양 | 비고 |
|---|---|---|
| 방치 정산 | 4h당 1개 (동료 랜덤) | 방치 보상에 수집 축 추가 — B1과도 시너지 |
| 차원 균열 | 회당 2개 (선택) | 일일 던전에 목적성 부여 |
| 주간 랭크 시험 | 승리당 3개 (선택) | 주간 루프 강화 |
| 성벽 10층마다 | 1개 | dodge 반복 동기 |
| 상점 (보석) | §3.3 | 주간 구매 제한 |

### 2.3 몬스터 도감 (가벼운 수집 축)

몬스터 종류별 처치 수 마일스톤(10/100/1,000) → 해당 종 **골드 드랍 +2%/4%/8% 영구**.
`totalKills`는 있으나 종별 카운트는 신설 필요. 보상이 방치 공식(M이 아닌 killGold 자체)에
붙어 도감이 "보는 재미"를 넘어 성장으로 이어진다.

### 2.4 일일 집중 장치

- **오늘의 첫 클리어 2배**: 콘텐츠별(사냥터 보스/원정/재련/비트) 하루 첫 성공 보상 2배
  — 4개 콘텐츠를 하루 한 번씩 도는 동선을 만든다 (일일 미션과 달리 수령 버튼 없이 자동)
- **주간 랭크 칭호**: 그림자 대전 누적 명성으로 칭호(견습 → 검객 → 검성 → 전설) —
  프로필에 표시, 시즌 리셋 없음 (서버가 없으므로 로컬 누적)

---

## 3. 결제 기획

### 3.1 원칙 — 확률형 전면 배제

**모든 상품은 확정 구매 또는 선택권이다. 뽑기(가챠)는 넣지 않는다.**

- 한국 게임산업법상 확률형 아이템은 확률 공시 의무 대상 — 공시·검증 운영 부담
- Apple/Google도 확률 고지 요구 — 심사 리스크
- 오프라인 싱글 게임이라 PvP 박탈감이 없어 확정 판매로도 충분히 동작
- 부수 효과: 스토어 등록의 데이터 보안·심사 노트가 단순해진다

과금의 역할은 **시간 단축과 수집 선점**으로 한정한다. 모든 유료 재화·동료는
비과금 경로(§2.2 조각 수급)로도 도달 가능해야 한다.

### 3.2 화폐 정리

- **골드**: 소프트 화폐 (현행 유지)
- **붉은 보석**: 하드 화폐. 현행 카탈로그 기준 환율 **1보석 ≈ ₩18.75** (₩1,500=80개)
- 보석 소비처 신설이 핵심 — 현재 보석은 사실상 사용처가 없다

### 3.3 상품 라인업

기존 카탈로그(보석 3종·모험가 세트 3종) 유지 + 아래 추가:

| 상품 | 가격 | 내용 | 종류 |
|---|---|---|---|
| **플레이어블 캐릭터: 흑요석 검사** | ₩5,900 | 전용 idle/공격 시트 + 패시브 "방치 효율 +1%p" | entitlement |
| **플레이어블 캐릭터: 새벽의 무희** | ₩5,900 | 전용 시트 + 패시브 "방치 시간 +30분" | entitlement |
| **동료: 성기사 루나 (SSR)** | 보석 900 (≈₩16,900) | 7번째 동료 확정 획득 ★1 · 광역+실드 컨셉 | 보석 소비 |
| **동료: 기계공 볼트 (SR)** | 보석 450 (≈₩8,400) | 8번째 동료 확정 획득 ★1 · 포탑 설치 컨셉 | 보석 소비 |
| **성급 조각 선택팩** | 보석 120 | 원하는 동료 조각 ×10 — **주당 동료별 3회 제한** | 보석 소비 |
| **방치 가속권 24h** | 보석 80 | 24시간 방치 산출 2배 (중첩 불가) | 보석 소비 |
| **원정 후원 계약 30일** | ₩5,500 | 매일 보석 20 + 방치 캡 +2h + 균열 +1회 | 월정액 lite |

설계 노트:
- **캐릭터 = 외형 + 미미한 패시브** (±1%p 수준). 전투력 판매가 아니라 정체성 판매.
  패시브가 0이면 구매 동기가 약하고, 크면 P2W — 1%p가 절충점
- **동료 직접 판매 + 성급은 파밍**: 획득(과금 가능)과 성장(파밍 필수)을 분리해
  "사면 끝"과 "안 사면 벽" 양쪽을 피한다. 신규 SSR도 조각은 §2.2 경로로만 승급
- **주간 구매 제한**: 조각팩 무제한이면 성급이 지갑 싸움이 된다. 제한이 과금 상한을
  만들어 장기 리텐션과 심사 인상 모두에 유리
- **월정액 lite**: 방치형 매출의 표준. 일할 지급이라 환불 계산도 단순

### 3.4 가격 검증 (보석 환율 일관성)

- 동료 SSR 900보석 ≈ ₩16,900 → 보석팩 1,200(₩15,000) + 80(₩1,500)으로 도달 가능 ✓
- 조각팩: ★5까지 490조각 = 49팩 = 5,880보석 ≈ ₩11만 — **전액 과금 시 상한**이 명확하고,
  주간 제한(동료당 3팩)으로 실제로는 최소 17주 분산 → 파밍 병행이 항상 이득 ✓

### 3.5 컴플라이언스 체크리스트

- [ ] 확률형 없음 → 확률 공시 불요 (본 문서가 그 근거 기록)
- [ ] IAP는 스토어 결제(StoreKit/Play Billing)만 사용 — 외부 결제 링크 금지
- [ ] 결제 정보는 스토어가 처리 — 앱의 "수집 없음" 방침 유지 가능 (구매 영수증 검증을
      서버 없이 로컬로 할 경우. 서버 검증 도입 시 방침 재작성)
- [ ] `payments/adapter.ts` 원칙 유지: **클라이언트에서 유료 재화를 직접 지급하지 않는다**
- [ ] 미성년자 결제 한도·청약철회는 스토어 정책 위임 + 고객센터(GitHub Issues) 안내
- [ ] 앱인토스판은 토스 IAP 규정 별도 — 초기에는 **네이티브판만 결제 오픈** 권장
- [ ] store/listing.md 데이터 보안 답변 재검토 (Play Billing 자체는 앱의 수집 아님)

---

## 4. 데이터 모델 영향 (스케치)

```ts
// CharacterProgress 추가
allyStars: Record<TitanHeroId | NewAllyId, number>;   // ★1~5, 미보유 0
allyShards: Record<TitanHeroId | NewAllyId, number>;
monsterKills: Record<TitanMonsterKind, number>;        // 도감
ownedCharacters: CharacterSkinId[];                    // 구매 캐릭터
activeCharacter: CharacterSkinId;
weeklyShardPacks: { week: string; bought: Record<string, number> }; // 구매 제한
firstClearDate: Record<"hunt"|"dodge"|"forge"|"beat", string>;      // 첫 클리어 2배
```

- `heroDps()`에 성급 배율 곱 추가, 환생 리셋은 `TitansSave` 초기화 함수 신설
- 신규 동료 2인은 `HEROES` 확장 + 시트 2칸 추가 (ally-roster 재생성 필요)
- 캐릭터 시트: hero-idle/attack과 동일 규격(4프레임) 2벌 × 2종 — AI 생성 파이프라인 재사용

## 5. 구현 로드맵 (권장 순서)

| 단계 | 내용 | 성격 |
|---|---|---|
| 1 | 성급·조각 데이터 모델 + 동료 성급 UI + 조각 수급 4경로 | 집중도 코어 |
| 2 | P3 환생 리워크 (리셋 + 결정 0.02 + 복귀 버프) | 밸런스 |
| 3 | P1 방치 따라잡기 진행 (**결정 변경 승인 후**) | 밸런스 |
| 4 | 벽 배너·벽 보상 (P2) + 몬스터 도감 + 첫 클리어 2배 | 집중도 |
| 5 | 시뮬레이터 확장 → 90일 전후 비교 재측정 | 검증 |
| 6 | 신규 동료 2인 (아트 + 스킬) | 콘텐츠 |
| 7 | 캐릭터 2종 (시트 생성 + 패시브) | 콘텐츠 |
| 8 | 상품 카탈로그 갱신 + Play Billing 어댑터 구현 | 결제 |

1~5는 결제 없이도 게임을 개선하므로 선행. 6~8은 스토어 심사 통과 후
첫 업데이트(v0.2.0)로 묶는 것을 권장 — 출시 심사는 가볍게, 수익화는 다음 릴리스에.

---

## 6. 미결정 — 승인 필요

1. **P1 방치 따라잡기** — 이전 "현행 유지" 결정의 변경 (§1.2)
2. **환생 리셋 강도** — 동료 레벨까지 리셋할지, 스테이지만 리셋할지 (§1.4는 전자)
3. **가격대** — §3.3 가격은 국내 캐주얼 기준 초안. 앱인토스판 동시 오픈 여부 포함
4. **신규 동료·캐릭터 컨셉** — 성기사/기계공/흑요석 검사/새벽의 무희는 자리표시자
